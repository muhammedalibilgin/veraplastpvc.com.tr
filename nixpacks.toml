[variables]
    NODE_VERSION = '22'
    NPM_CONFIG_LEGACY_PEER_DEPS = 'true'

[phases.install]
    cmds = ['npm ci --legacy-peer-deps']

[phases.build]
    cmds = ['npm run build']

[phases.start]
    command = "npm run start-custom"    